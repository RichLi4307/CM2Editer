# CM2Editer 问题清单

> 生成时间：2026-07-07 14:34
> 更新时间：2026-07-07 17:07
> 说明：本清单中 P0–P3 的遗留问题已在本轮开发中解决。以下保留问题描述用于追溯，末尾生成临时检查单供手工验证。
> 优先级：P0（阻塞可用性）→ P1（体验问题）→ P2（ polish / 待验证）

---

## P0：交互阻塞（功能不完整，影响主要 workflow）

### P0.1 框选视觉与风格错误 ✅ 已解决

**症状**：

- 空白处左键拖拽框选时，画布不显示选择框矩形。
- Shift/Ctrl + 空白处左键拖拽当前为 cross 风格（相交即选中），但需求为 window 风格（完全包含才选中）。
- 在节点上按住 Shift/Ctrl 再拖拽，仍然进入单节点拖动，而不是以该节点为起点开始框选。

**解决方式**：

- `src/ui/interaction.rs` 中 `BoxSelecting` 状态增加 `mode` 字段，按拖拽方向决定：左→右为 `Window`（严格包含），右→左为 `Crossing`（相交）。
- 拖拽过程中绘制半透明选择框（Window=蓝色，Crossing=绿色）。
- 节点上按住 Shift/Ctrl 拖拽时进入框选模式，不触发单节点拖动。
- Shift 为追加、Ctrl 为减选、无修饰键为替换选集。

---

### P0.2 多选节点无法整体拖拽 ✅ 已解决

**症状**：框选多个节点后，只能拖动鼠标按住的那个节点，其他被选节点不会跟随移动。

**解决方式**：`DraggingNode` 状态记录所有被选中节点的起始位置，拖拽时同步偏移；释放后为每个实际移动的节点生成 `MoveNode` 命令。

---

### P0.3 连线无法选中/删除 ✅ 已解决

**症状**：

- 鼠标悬停或单击连线无法将其选中。
- 框选（cross/window）无法选中连线。
- 选中连线后按 Delete 无反应。

**解决方式**：

- `EdgeRenderer` 新增 `hit_rect` 方法，对贝塞尔曲线采样并返回外接命中框。
- `app.rs` 渲染边时收集 `edge_hits` 并传入 `handle_input`。
- 单击连线选中、框选包含连线、选中后按 Delete 删除，`delete_selected` 已支持删除边。

> 附带建议：连线仍保持贝塞尔曲线，未改为电路连接线风格；本次优先补全交互，风格变更可在后续设计讨论中决定是否同步。

---

### P0.4 端口高亮/吸附与不兼容提示缺失 ✅ 已解决

**症状**：

- 从输出端口拖线时，兼容输入端口不会高亮，也没有吸附效果。
- 拖到不兼容端口时不会变红，也没有视觉拒绝提示，仅松手后状态栏提示“目标端口不兼容或无效”。

**解决方式**：

- `DrawingEdge` 状态实时检测鼠标下端口，按兼容/不兼容/已占用/成环返回 `PortTargetStatus`。
- 兼容端口显示高亮圆环；不兼容/成环/已占用显示对应颜色（红/橙）。
- 临时拖线颜色随目标状态变化。
- 释放时拒绝非法连接（自环、成环、类型不兼容、Data 端口已占用）。

---

## P1：右键菜单与节点操作

### P1.1 右键菜单复制/粘贴未落地 ✅ 已解决

**症状**：

- 右键节点菜单中的“复制”仅选中该节点，并未复制到剪贴板。
- 复制后没有“粘贴”选项。
- Ctrl+V 无法粘贴节点。

**解决方式**：

- 新增 `Clipboard { nodes, edges }` 结构，`App` 持有该字段。
- “复制”将当前选区节点及节点间连线写入剪贴板，并重新生成 ID。
- “粘贴”在右键菜单位置或鼠标悬停处创建副本，节点间连线同步重建。
- 工具栏与全局快捷键支持 Ctrl+C / Ctrl+V。

---

### P1.2 右键删除节点未级联删除边 ✅ 已解决

**症状**：右键节点 → 删除，仅删除该节点，其关联边仍残留在图中和 JSON 里。

**解决方式**：右键菜单“删除”收集该节点的所有关联边，以 `Command::RemoveNode { node, edges }` 入栈，撤销时可恢复节点及其边。

---

## P2：文件与数据一致性

### P2.1 加载 JSON / 导出 `.code` 未接入 GUI ✅ 已解决

**症状**：

- 顶部工具栏只有“保存 (Ctrl+S)”“导出 JSON”“导出 .code”按钮。
- 没有“打开/加载 .json”按钮或菜单。

**解决方式**：

- 工具栏新增“打开 JSON”按钮，使用 `rfd` 弹出文件选择对话框，加载后替换当前图并恢复视口。
- 保存 JSON 的 `version` 由 `serializer::migration::CURRENT_VERSION` 控制（当前为 `"1.3"`）。
- 导出 `.code` 与 `docs/examples/` 的对照由现有 `tests/code_gen.rs` 覆盖。

---

### P2.2 空画布/欢迎页未处理 ✅ 已解决

**症状**：程序启动后默认加载 3 个示例节点（Start → If → Log），无空画布状态或欢迎页。

**解决方式**：

- `App::new` 初始化为空图。
- 节点为空时画布中央显示提示：“按 Space 添加第一个节点\n或从左侧节点库选择”。
- 工具栏新增“新建”按钮可清空当前图。

---

## P3：极端场景与验证器可视化

### P3.1 环检测无画布提示 ✅ 已解决

**症状**：故意连接成环（A→B→A）时，底部状态栏显示错误文字，但画布区域无任何高亮或提示。

**解决方式**：

- `GraphValidator::check_no_cycles` 返回环上节点 ID 列表，`FlowError::CycleDetected(Vec<String>)`。
- `App::validate` 提取受影响节点 ID 到 `error_nodes`。
- 错误节点渲染红色边框；状态栏显示错误数量。

---

### P3.2 复制粘贴性能未验证 ⚠️ 未自动验证

**症状**：未测试复制粘贴 100 个节点后帧率是否保持 60fps。

**说明**：复制/粘贴实现本身为 O(n)，未引入每帧重计算。性能验证需手动运行 `cargo run` 并复制大量节点观察帧率。已加入临时检查单。

---

### P3.3 损坏 JSON 加载未处理 ✅ 已解决

**症状**：加载损坏或版本不支持的 JSON 时，程序可能 panic 或无任何友好提示。

**解决方式**：`load_json` 捕获读取/解析/迁移错误，通过 `status_message` 显示“加载失败: ...”，程序不 panic。已加入临时检查单做手动验证。

---

## 临时检查单（需手工验证）

### 交互

- [pass] 框选：空白处左键拖拽，出现蓝色半透明选择框，方向左→右仅选中完全包含的节点
- [?] 框选：右→左拖拽，出现绿色选择框，相交即选中 > 应为虚线框，测试结果为实线框,cross正常
- [pass] 框选：按住 Shift 空白处拖拽追加选中；Ctrl 空白处拖拽减选
- [pass] 框选：在节点上按住 Shift/Ctrl 拖拽开始框选，不触发节点拖动
- [?] 多选：框选多个节点后拖动其中一个，所有选中节点同步移动 > 功能实现正常，但是被同步拖动的其他节点，会被未被拖动的节点遮挡，即多选拖动时其他被联动的节点没置顶
- [?] 连线：单击连线可选中（高亮），按 Delete 删除，JSON 预览同步更新 > 线本身偏白，高亮不明显，只有加粗，且线的图层太高挡住高亮了。建议线仅比连接的节点高一层，然后高亮再高一层，然后其他节点在移动过程中如果经过线，拖动的节点就比线和高亮再高一层
- [pass] 框选：框选矩形覆盖连线时，连线被选中
- [pass] 拖线：从输出端口拖线，兼容输入端口高亮/放大 > 放大是端口圆外面长出了线的颜色的环，方案不错但是建议与档案比对，个人建议保留该视觉效果
- [?] 拖线：拖到不兼容/已占用/会成环的端口，端口或连线变红/橙，释放后拒绝连接 > 连线与端口外的环成功变色，目标端口本身不变色，同样与档案对比，个人建议与上一条的现象同步修改或保留
- [false] 右键：右键节点“复制”复制当前选区，“粘贴”在菜单位置创建副本 > 无法在空白处右键，仅能在节点上右键打开复制/粘贴菜单，且粘贴后新节点图层在刚刚被右键的节点上方图层，但是现在拖动重叠部分，是下面的被右键的节点被拖动
- [false] 快捷键：Ctrl+C 复制选中节点，Ctrl+V 在鼠标位置粘贴 > 均失效
- [pass] 右键删除：删除节点时其关联边同步消失，撤销可恢复

> 空格能打开搜索框，但是搜索框无常见关闭渠道,仅能点选节点后自动关闭
> 点选能选连线，但是无法选节点，直接在节点上按下并拖动，节点能正常被选择并拖动，点击空白处能取消选择
> 建议鼠标靠近输出端口时能像拖线放大那样放大出一个环，体现吸附效果

### 文件与数据

- [false] 保存 JSON 后 `version` 为 "1.3" > cargo run运行，打开文件按钮能正常唤起文件管理器，但按下保存按钮未唤起文件管理器
- [失败或无法测试] 工具栏“打开 JSON”可加载保存的文件，节点位置、连线、参数一致 > cargo run运行，“打开JSON”按钮能正常唤起文件管理器，但按下保存按钮未唤起文件管理器，没有范例json所以无法测试
- [false] 工具栏“导出 .code”生成文件，内容可被测试/游戏加载 > cargo run运行，打开文件按钮能正常唤起文件管理器，但按下保存按钮未唤起文件管理器
- [pass] 启动后为空画布，显示欢迎提示；按 Space 或左栏可添加节点 > 欢迎提示就是画布灰色文字“按 Space 添加第一个节点或左栏可以选择”，如果你之前是计划做开始页，那我这里没有看见任何其他弹窗
- [pass] 工具栏“新建”清空当前图 > 一键清空，没有保存提示，成功出现“开始”画布

### 验证与极端场景

- [pass] 连接成环（A→B→A）后，相关节点边框变红，状态栏显示错误
- [失败或无法测试] 故意加载损坏 JSON，程序不 panic，状态栏显示加载失败 > cargo run运行，“打开JSON”按钮能正常唤起文件管理器，但按下保存按钮未唤起文件管理器，没有范例json所以无法测试
- [false] 复制粘贴 100 个节点，观察 `cargo run` 是否卡顿（目标 60fps）> 添加100个开始节点（理论上很轻量）后，帧率低于20，无论节点是否在屏幕空间内。显卡功耗无明显变化

> 意外发现开始节点S、操作A、操作B可以这样连接：graph1(S→B) graph2(S→A→B)且无任何提示，应⚠️警告但不阻止，如果警告实现复杂，可以移入todo或interaction_spec.md，未来解决

---

## 当前建议修复顺序

所有 P0–P2 与 P3.1、P3.3 已解决；剩余可进入下一轮的工作：

1. 根据临时检查单完成手工验证并修复回归。
2. 将连线由贝塞尔曲线改为电路连接线风格（如需要）。
3. 状态栏错误点击展开详情列表（当前仅显示数量）。
4. 完成复制粘贴性能压测与正式测试用例。
5. 空画布下自动创建 Start 节点（可选，按 `docs/interaction_spec.md` 4.1 节）。

## TODO / 未来计划

- **Start 节点多路连接警告**：当存在多个 Start 节点都连接到同一流程时（如 S1→B, S2→A→B），应显示 ⚠️ 警告但不阻止。当前无任何提示，可能造成逻辑混淆。若实现复杂可延后。

---

## 最近旧清单归档位置

- `docs/archive/紧急修复清单-2026-07-07-14-34.md`
- `docs/archive/紧急修复清单2026-07-07-0248.md`
