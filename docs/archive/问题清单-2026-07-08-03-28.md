# CM2Editer 问题清单（归档）

> 生成时间：2026-07-07 22:46
> 归档时间：2026-07-08 03:28
> 状态：本轮测试全部通过，转入最终清单合并

---

## 本轮修复（6 项，2026-07-07 22:46）

| 优先级 | 问题 | 修复文件 | 说明 |
|--------|------|----------|------|
| P0 | Log 节点 output 只能输入数字 | `src/api/definitions.rs` | `output` 参数类型从 `List` 改为 `String`，新建 Log 节点默认 `""` 可正常输入字符串 |
| P1 | 空图导出无警告 | `src/app.rs` | 导出 `.code` 后若图为空，弹出提示"图为空，导出的 .code 文件内容为空" |
| P2 | 多 Start 节点合并到单一 `main` | `src/code_gen/generator.rs` | 无显式标签时，每个 Start 节点生成独立标签 `main`、`main_1`、`main_2`… |
| P2 | 属性面板空格唤出搜索 | `src/app.rs` | Space 快捷键仅在 `!ctx.wants_keyboard_input()` 时触发，文本框输入空格不再误触 |
| P3 | 折叠节点高度未缩小 | `src/ui/node_renderer.rs` | 折叠时节点高度自适应为标题栏 + 端口所需最小高度 |
| P3 | 导出 JSON 后 Ctrl+S 仍保存旧路径 | `src/app.rs` | 导出 JSON 成功后更新 `current_file`，后续保存直接写入导出文件 |

---

## 第三轮测试结果（全部通过）

### 节点参数编辑

- [pass] 新建 Log 节点，在属性面板 output 输入任意字符串（如 `hello world`），不会变回 `[]`
- [pass] 加载 `tests/test1.json`，Log 节点 output 仍显示并编辑为 `hello`
- [pass] 撤/重做参数修改正常

### 核心导出

- [pass] 创建 1 个 Start → If → Log，导出 `.code` 正常 → `test2.json/code`
- [pass] 加载 `tests/test1.json` 导出 `.code` 正常
- [pass] 创建 2 个独立 Start 流程（S1→A→B 和 S2→C→D），导出 `.code` 出现 `main:` 和 `main_1:` 两个独立标签
- [pass] 打开 `tests/test4.json`（空图），导出 `.code` 时弹出"图为空"警告，且文件内容为空

### 搜索与快捷键

- [pass] 在右侧属性面板文本框中按空格，**不**打开搜索窗口
- [pass] 在画布空白处按空格，正常打开搜索窗口
- [pass] 搜索窗口输入框中按空格正常输入

### 折叠与视觉

- [pass] 创建 Log 节点，点击折叠后节点高度明显缩小（仅标题栏 + 端口区域）
- [pass] 展开后恢复原有高度
- [pass] 端口标签在折叠/展开状态下均可见

### 文件与保存

- [pass] 新建图，点击"导出 JSON"选择新路径，导出成功后按 Ctrl+S 直接保存到同一文件（无需再次选择路径）
- [pass] 打开旧 JSON 后"导出 JSON"到新路径，Ctrl+S 也更新到新路径

### 数据流（当前预期行为）

- [pass] If 节点 condition 仍只能手动勾选（待 DataFlow 重构完成后验证）

---

## 测试备注

> log 节点填充数据后清空，会变成 null 并在底部显示 1 个错误
> General Function 类型的节点，数据参数类型模糊或错误，查阅 docs 中的 API 文档进行重构，可枚举的变量改成下拉表
> 我提供了 selected_cosplay.json 在 docs 文件夹，这个是 cosplay 服装参数的命名空间，考虑到内容太多，在节点涉及填入这些参数时，提供一个新窗口，可以选择、搜索、显示译名和命名空间。
> 左栏应分二级菜单，当前使用的节点表被归纳到节点菜单，一级菜单包括文件操作菜单入口、命名空间管理入口（振动器、活塞强度这些都是可枚举的，需要一个快速导入导出和复制的地方），格式建议匹配 selected_cosplay.json。然后是坐标菜单，可以快速管理坐标、拖出预制成坐标变量、视角方向的全局/局部变量
> 这里点名传送、坐标获取节点等。考虑到 .code 是一种编码语言，我们可以预制一些"语言糖"，比如坐标变量、视角方向，在编辑器里可以便捷更改 x、y、z 参数，但在生成 .code 时需要进行合适的语言转译。该想法需要仔细评估可行性。因为本质上是节点化的 .code 代码块编辑器，所以甚至有可能与数学、变量类型功能的节点冲突。这方面需要我们自己的 API 文档
> 给 json 预览栏变成可调高度的
