# CM2Editer 文档架构 — 第二审计意见

> 基于 Python 对 7 份文档的交叉提取与比对，现给出第二审计意见。
> 审计时间：2026-07-06  
> 审计范围：`agent_prompt.md`、`node_types.md`、`json_schema.md`、`rust_project_skeleton.md`、`TODO.md`、`Cargo.toml`、`README.md`  
> 审计工具：Python 正则提取 + 人工复核

---

## 总体结论：**有风险**

文档间存在 **3 项 P0 级数据冲突**、**4 项 P1 级模型错位**、**5 项 P2 级规划漂移**。其中 NodeType 数量矛盾与 `threads` 默认值的非法 JSON 格式会直接导致序列化/反序列化代码与文档对不上，需在三审前修复。

---

## 一、P0 必查项（严重冲突）

### 1. 版本基线：Rust Edition 不一致（已记录，由 Agent 适配）

| 文档 | 声明 Edition |
|------|-------------|
| `Cargo.toml` | **2024** |
| `agent_prompt.md` | 2021 |
| `rust_project_skeleton.md` | 2021 |
| `README.md` | 2024 |

- **冲突点**：`agent_prompt` 与 `rust_skeleton` 明确要求 "Rust 2021 Edition"，但 `Cargo.toml` 实际使用 `edition = "2024"`。
- **影响范围**：代码生成、CI 配置、文档描述。
- **建议动作**：**按用户指令，此差异由 Agent 结合已有代码（Phase 1 已完成）自行适配**，无需文档修改。但需在 `agent_prompt.md` 和 `rust_skeleton.md` 中追加备注："实际项目使用 2024 Edition，文档中的 2021 为模板基线，以 `Cargo.toml` 为准"。

---

### 2. 核心状态模型：`threads` 默认值格式非法

| 文档 | 声明 |
|------|------|
| `json_schema.md` | `threads` 默认值列写为 `` `[{main}]` `` |

- **冲突点**：`[{main}]` **不是合法 JSON**。`main` 未加引号，对象缺键名。
- **影响范围**：`serializer/migration.rs` 的默认值回退逻辑、加载器解析。
- **建议动作**：将 `json_schema.md` 默认值修正为合法 JSON 数组：
  ```json
  [{"id": "thread_main", "name": "main", "entry_label": "main", "parent": null, "auto_start": true}]
  ```
  同时核对 `agent_prompt.md` 中 JSON 示例的 `threads` 字段是否与此一致。

---

### 3. 单一事实源：`NodeType` 数量矛盾（103 vs 174）

| 文档 | 声明数量 | 实际提取 |
|------|---------|---------|
| `rust_project_skeleton.md` | 103 种 | — |
| `TODO.md` | 103 种 | — |
| `node_types.md` | — | **174 个** 节点名（含 60 个 `Object.Method`、19 个 `Create*`、10 个控制流、85 个其他函数） |

- **冲突点**：`graph/types.rs` 的 `NodeType` 枚举若按 103 实现，会遗漏 `node_types.md` 中 71 个节点（主要是对象方法如 `Area.Inside`、`NPC.Warp` 等）。
- **影响范围**：`api/definitions.rs` 注册表、验证器节点类型检查、代码生成器模板匹配。
- **建议动作**：
  1. 明确 `NodeType` 枚举的计数规则：是 **103 个基础类型**（不含对象方法重载）还是 **174 个全量节点**？
  2. 若对象方法（如 `NPC.Warp`）作为独立枚举变体，则 `rust_skeleton.md` 和 `TODO.md` 中的 "103 种" 必须修正为 "174 种"。
  3. 若对象方法通过 `(ObjectType, MethodName)` 组合表示，则需在 `node_types.md` 中补充该设计说明，并统一 `agent_prompt.md` 中 `NodeType` 与字符串的映射规则。

---

### 4. 执行语义：验证器检查清单在 `agent_prompt.md` 中缺失

| 文档 | 验证器描述 |
|------|-----------|
| `json_schema.md` | 13 条完整检查清单（含环检测、孤立节点、必填参数等） |
| `agent_prompt.md` | 仅提及 `GraphValidator::validate`，无清单 |
| `agent_prompt.md` | 错误处理表格 4 条，与验证器清单不对应 |

- **冲突点**：`agent_prompt.md` 作为 Agent 开发指令集，未包含 `json_schema.md` 第 10 节的 13 条验证规则，Agent 可能遗漏"孤立节点检测"或"同一节点不重复出现在同一线性序列中"等检查。
- **影响范围**：验证器实现完整性。
- **建议动作**：将 `json_schema.md` 的 13 条验证清单以精简形式写入 `agent_prompt.md` 第 3.3 节或第 8 节（常见陷阱）。

---

## 二、P1 高优先级（模型错位）

### 5. 图数据模型与序列化模型字段不对齐

| 字段 | `json_schema.md` | `agent_prompt.md` JSON 示例 | 差异 |
|------|-------------------|---------------------------|------|
| `category` | ✅ 存在（可选，从类型推导） | ❌ 缺失 | agent_prompt 示例未体现 |
| `collapsed` | ✅ 存在（可选，默认 false） | ❌ 缺失 | agent_prompt 示例未体现 |
| `waypoints` | ✅ Edge 可选字段 | ❌ 缺失 | agent_prompt 示例未体现 |
| `size` | ✅ 两者都有 | ✅ 两者都有 | 一致 |

- **影响范围**：JSON 序列化往返测试、UI 状态恢复。
- **建议动作**：更新 `agent_prompt.md` 第 4 节的 JSON 示例，加入 `category`、`collapsed`、`waypoints`，使其与 `json_schema.md` 完全一致。

---

### 6. 特殊节点（Meta / Comment / Group）在 `node_types.md` 外完全失联

| 文档 | Meta | Comment | Group |
|------|------|---------|-------|
| `node_types.md` | ✅（第 11 节 + 附注双定义） | ✅ | ✅ |
| `agent_prompt.md` | ❌ 未提及 | ❌ 未提及 | ❌ 未提及 |
| `json_schema.md` | ❌ 未提及 | ❌ 未提及（无 `comments` 字段） | ❌ 未提及 |
| `rust_skeleton.md` | ❌ 未提及 | ❌ 未提及 | ❌ 未提及 |

- **冲突点**：
  1. `node_types.md` 第 11 节和**附注**分别定义了 `Meta`、`Comment`、`Group`，但附注的表格格式与第 11 节的文本描述重复。
  2. `json_schema.md` 未定义 `comments` 顶层字段，与 `node_types.md` 附注中"在 JSON 中保留以便编辑体验"的建议冲突。
  3. `agent_prompt.md` 的 JSON 契约、验证器、代码生成章节均未说明如何处理这些特殊节点，Agent 可能将其误认为可执行节点。
- **影响范围**：序列化、验证器、代码生成（Comment 不应生成代码，Group 不应影响拓扑）。
- **建议动作**：
  1. 删除 `node_types.md` 附注中与第 11 节重复的 `Meta`/`Comment`/`Group` 表格，合并到第 11 节。
  2. 在 `json_schema.md` 顶层结构中加入可选的 `comments` 数组字段（或说明放入 `nodes` 中作为特殊类型）。
  3. 在 `agent_prompt.md` 第 8 节"常见陷阱"中新增一条："特殊节点（Meta/Comment/Group）不参与代码生成，验证器应跳过其拓扑检查"。

---

### 7. `ParamValue` 类型系统跨文档口径不一

| 文档 | 声明 |
|------|------|
| `agent_prompt.md` | `params` 中 `value` 为空时表示读取，非空表示写入（针对 Global/Local/NPC.Stopped 等） |
| `json_schema.md` | 参数值类型映射表包含 `Ref` 类型：`{"ref": "node_002", "port": "out_result"}` |
| `rust_skeleton.md` | `ParamValue` 结构体包含 `Literal/Ref/Null` |

- **冲突点**：`agent_prompt.md` 未提及 `ParamValue::Ref` 的 JSON 表示形式，Agent 可能不知道端口连接在 JSON 中如何序列化。
- **影响范围**：JSON 序列化、UI 连线逻辑。
- **建议动作**：在 `agent_prompt.md` 第 4 节 JSON 契约中，补充 `ParamValue::Ref` 的示例（如 `{"ref": "node_xxx", "port": "out_yyy"}`）。

---

### 8. `README.md` 与 `TODO.md` 进度差异（仅列差异点，不评级）

| 维度 | `README.md` | `TODO.md` | 差异 |
|------|-------------|-----------|------|
| 数据层状态 | "已完成数据层" | Phase 1 全部 ✅（1.1-1.4，含 34 项测试） | **基本一致** |
| api 层 | 未明确提及 | 1.1.3/1.1.4 ✅ 已完成 | README 未同步 api 完成状态 |
| 测试数量 | 未提及 | 明确记录 "34 项测试通过" | README 未体现测试覆盖度 |
| 示例任务 | 提及 4 个示例 | Phase 2.2.9 待实现 | README 提前声明了未验证的示例 |
| GUI 状态 | "cargo run 暂时只进入占位入口" | Phase 3 全部待实现 | 一致 |

- **建议动作**：README 最后写即可，当前无需同步。

---

## 三、P2 中优先级（规划漂移）

### 9. `Cargo.toml` 与 `rust_project_skeleton.md` 依赖严重脱节

| 差异项 | `rust_skeleton.md` 声称 | 实际 `Cargo.toml` | 状态 |
|--------|------------------------|-------------------|------|
| `anyhow` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `tracing` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `clap` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `config` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `glam` | ✅ 应存在 | ❌ 缺失 | **TODO 说 Vec2 自定义实现，与此矛盾** |
| `tempfile` (dev) | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `pretty_assertions` (dev) | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `[lints]` | ❌ 未提及 | ✅ 存在 | 文档缺失 |
| `authors` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `license` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `description` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |
| `repository` | ✅ 应存在 | ❌ 缺失 | 待补充或文档删除 |

- **建议动作**：按用户指令，**由 Agent 结合具体代码决定是补依赖还是删文档**。但需在 `rust_project_skeleton.md` 顶部追加备注："Cargo.toml 以仓库实际文件为准，本文档中的依赖列表为规划草案"。

---

### 10. `Vec2` 实现方式矛盾

| 文档 | 声明 |
|------|------|
| `rust_project_skeleton.md` | 使用 `glam` 依赖 |
| `TODO.md` | "Vec2 由自定义结构体实现，未采用 glam" |

- **建议动作**：Agent 已按 TODO 用自定义实现，只需更新 `rust_project_skeleton.md` 删除 `glam` 并注明 `Vec2` 为自定义。

---

### 11. 颜色编码单一来源风险

| 文档 | 状态 |
|------|------|
| `node_types.md` | 完整 11 类颜色编码表 |
| `agent_prompt.md` | 仅引用"见 `node_types.md` 颜色编码表" |
| `rust_skeleton.md` | 仅引用 `theme.rs` |

- **风险**：若 `node_types.md` 颜色变更，`theme.rs` 可能未同步。
- **建议动作**：在 `theme.rs` 中添加注释："颜色来源：docs/node_types.md 第 12 节"，建立反向追踪。

---

### 12. 目录结构缺少 `app/` 模块

| 文档 | 目录结构 |
|------|---------|
| `agent_prompt.md` | 明确列出 `app/` 模块（主循环与状态管理） |
| `rust_project_skeleton.md` | `src/` 下无 `app/` 目录，仅有 `app.rs` 文件 |

- **建议动作**：按用户指令，由 Agent 结合代码决定是建 `app/` 目录还是保持 `app.rs` 平铺。文档需同步。

---

### 13. `node_types.md` 附注与第 11 节重复

`Meta`、`Comment`、`Group` 在"十一、特殊节点"和"附注"中各定义一次，附注的表格格式与第 11 节文本描述冗余。

- **建议动作**：删除附注中的重复表格，保留第 11 节为权威定义。

---

## 四、审核结论模板（按 checklist 要求）

| 项目 | 结论 | 主要冲突 | 影响范围 | 建议动作 |
|------|------|---------|---------|---------|
| **P0-1 版本基线** | 通过（已记录） | Edition 2024 vs 2021 | 文档一致性 | Agent 按代码适配，文档加备注 |
| **P0-2 状态模型闭合** | 有风险 | `threads` 默认值 `[{main}]` 非法 JSON | 序列化/加载器 | 修正为合法 JSON 数组 |
| **P0-3 执行语义** | 需重构 | 验证器 13 条清单在 agent_prompt 中缺失 | 验证器实现 | 将清单写入 agent_prompt |
| **P1-1 单一事实源** | 需重构 | NodeType 103 vs 174 | 注册表/枚举/代码生成 | 统一计数规则并修正文档 |
| **P1-2 序列化对齐** | 有风险 | `category`/`collapsed`/`waypoints` 在 agent_prompt 示例中缺失 | JSON 往返测试 | 更新 agent_prompt JSON 示例 |
| **P1-3 README/TODO** | 通过 | 仅测试数量、api 状态未同步 | 对外说明 | 最后写 README，当前忽略 |
| **P2-1 规划稿标注** | 有风险 | rust_skeleton 未标注为规划稿 | 依赖管理 | 顶部加备注 |
| **P2-2 默认值一致** | 有风险 | `[{main}]` 非法 + `meta` 透传规则未在 agent_prompt 强调 | 迁移逻辑 | 修正默认值格式 |
| **P2-3 文档链接** | 通过 | 未发现明显失效路径（无目录结构无法完全确认） | — | 定期核对 |

---

## 五、给 Agent 的三审修复优先级

1. **立即修复（阻塞开发）**：
   - 统一 `NodeType` 计数规则（103 还是 174？）
   - 修正 `json_schema.md` `threads` 默认值格式

2. **本轮修复（影响序列化）**：
   - 在 `agent_prompt.md` 中补充验证器 13 条清单
   - 在 `agent_prompt.md` JSON 示例中加入 `category`、`collapsed`、`waypoints`
   - 明确 `Meta`/`Comment`/`Group` 的序列化规则（跳过代码生成）

3. **后续修复（文档同步）**：
   - `Cargo.toml` 与 `rust_project_skeleton.md` 依赖对齐
   - `Vec2` / `glam` 矛盾消除
   - `README.md` 最后统一更新

---

**审计人**：Kimi（第二审计）  
**置信度**：高（基于 Python 提取 + 全文交叉比对）
