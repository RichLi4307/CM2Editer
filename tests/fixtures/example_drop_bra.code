var_main_thread = CreateThread("main")
main:
	var_node_ct_out_thread = CreateThread("main")
	var_node_cond_out_condition = CreateCondition("[Exposed_All]", id="exposed")
	if true
		Log(output="bra off")
	else
		Log(output="bra on")
	Log(output="done")
	_result = null
